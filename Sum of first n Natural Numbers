//Sum of first n Natural Numbers
#include<bits/stdc++.h>
using namespace std;
int sum(int k)
{
    if(k>0)
    {
        return k+sum(k-1);
    }
    return 0;
}

int main()
{
    int k=10;
    cout<<sum(k);
}
